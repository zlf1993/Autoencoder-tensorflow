import numpy as np
from scipy import *
from scipy.linalg import norm, pinv
 
from matplotlib import pyplot as plt

centers = [random.uniform(-1, 1, 5) for i in range(10)]
G = zeros((2, self.numCenters), float)
print(G)

